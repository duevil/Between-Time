using UnityEngine;

public class Atztec_Numbers : MonoBehaviour
{
    int count;
    string solution;
    string input;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // grabable machen
        // erscheinen lassen
        print("Button pressed");
        input = "";
    }

    void buttonPressed(string number)
    {
        input += number;

        // if Rätsel gelöst -> weiter
    }
}
